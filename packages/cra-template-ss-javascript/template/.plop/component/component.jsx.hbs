import React from 'react';
import * as styles from './{{kebabCase name}}.component.styles';

export const {{pascalCase name}} = ({}) => {
  return (
    <div className={styles.defaultStyle}>{{pascalCase name}}</div>
  );
};