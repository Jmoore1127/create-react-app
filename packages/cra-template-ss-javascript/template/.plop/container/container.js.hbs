import { compose } from 'recompose';
import { connect } from 'react-redux';
import { {{pascalCase name}} } from './{{kebabCase name}}.component';

export const {{pascalCase name}}Container = compose(
    connect(
        (state) => ({}),
        {}
    ),
)({{pascalCase name}});