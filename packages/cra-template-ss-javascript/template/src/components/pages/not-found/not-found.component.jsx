import React from 'react';
import * as styles from './not-found.component.styles';

export const NotFound = () => (
  <div css={styles.errored}>
    Sorry the page you were looking for couldn't be found!
  </div>
);