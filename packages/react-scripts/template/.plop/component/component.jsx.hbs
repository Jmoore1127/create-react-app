import React from 'react';
import styles from './{{kebabCase name}}.component.module.css';

export const {{pascalCase name}} = ({}) => {
  return (
    <div className={styles.defaultStyle}>{{pascalCase name}}</div>
  );
};